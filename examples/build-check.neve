#!/usr/bin/env neve
import std.io as io;

let build = io.execCommand(io.command("cargo", ["build", "--workspace"]));
if io.processSuccess(build) then {
    let test = io.execCommand(io.command("cargo", ["test", "--workspace"]));
    io.processSuccess(test)
} else false
