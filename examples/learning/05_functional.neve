use std.list = list

factorial(n: Int) -> Int = {
  if n <= 1 -> 1 else n * factorial(n - 1)
}

double(x: Int) -> Int = x * 2

increment(x: Int) -> Int = x + 1

doubled = list.map(double, [1, 2, 3])

filtered = list.filter(|x| {
  x > 5
}, [3, 6, 9])

folded = list.fold(0, |acc, x| {
  acc + x
}, [1, 2, 3])

fact5 = factorial(5)

piped = 10 |> double |> increment
