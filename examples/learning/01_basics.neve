type UserId = Int;

type Point =  {
  x: Float,
  y: Float,
};

type Color =  {
  | Red
  | Green
  | Blue
  | RGB(Int, Int, Int)
}

add(x: Int, y: Int) -> Int = x + y

double(x: _) = x * 2

greet(name: String) -> String = {
  let greeting = `Hello, {name}!`;
  greeting
}

multiply = |x, y| x * y

answer = 42

pi: Float = 3.14159

message = "Neve is fun!"

flag = true

unit = ()

name = "World"

welcome = `Hi {name}, the answer is {add(40, 2)}`

status = if answer > 0 then "positive" else "non-positive"

piped = 10 |> double |> double

numbers = [1, 2, 3, 4, 5]

empty_list: List<Int> = []

combined = [1, 2] ++ [3, 4]

evens = [x | x <- numbers, x % 2 == 0]

doubled = [x * 2 | x <- numbers]

point = { x = 10, y = 20 }

origin = { x = 0, y = 0 }

x_val = 5

y_val = 7

quick = { x = x_val, y = y_val }

moved = point & { x = 15 }

point_x = point.x

result = { answer = answer, doubled = doubled, evens = evens, point = moved, piped = piped }
