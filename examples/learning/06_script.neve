#!/usr/bin/env neve run
use std.io = io

main() = {
  (args, _) = io.args()
  name = match args {
    | [n, ..] -> n
    | [] -> "World"
  }
  io.println("Hello, " ++ name ++ "!")
  files = io.glob("*.neve")
  count = len(files)
  io.println("Found " ++ toString(count) ++ " .neve files")
  0
}
