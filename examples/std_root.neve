use std.list;
use std.string;

let nums = list.range(1, 3);
let total = list.len(nums);

let words = ["root", "import"];
let joined = string.join(words, " ");
let result = #{ total = total, joined = joined };
