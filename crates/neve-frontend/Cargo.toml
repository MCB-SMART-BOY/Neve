[package]
name = "neve-frontend"
description = "Frontend analysis pipeline for Neve (parse + lower + type check)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
neve-common = { version = "5.0.0", path = "../neve-common" }
neve-diagnostic = { version = "5.0.0", path = "../neve-diagnostic" }
neve-parser = { version = "5.0.0", path = "../neve-parser" }
neve-hir = { version = "5.0.0", path = "../neve-hir" }
neve-syntax = { version = "5.0.0", path = "../neve-syntax" }
neve-typeck = { version = "5.0.0", path = "../neve-typeck" }
