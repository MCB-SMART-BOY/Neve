[package]
name = "neve-parser"
description = "Parser for Neve"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
neve-common = { version = "5.0.0", path = "../neve-common" }
neve-diagnostic = { version = "5.0.0", path = "../neve-diagnostic" }
neve-lexer = { version = "5.0.0", path = "../neve-lexer" }
neve-syntax = { version = "5.0.0", path = "../neve-syntax" }
