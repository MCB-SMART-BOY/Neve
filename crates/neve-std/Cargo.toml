[package]
name = "neve-std"
description = "Standard library for Neve"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
glob = "0.3"
rpassword = "7"
neve-common = { version = "4.0.2", path = "../neve-common" }
neve-eval = { version = "4.0.2", path = "../neve-eval" }
neve-derive = { version = "4.0.2", path = "../neve-derive" }
neve-fetch = { version = "4.0.2", path = "../neve-fetch" }
sha2.workspace = true

[target.'cfg(unix)'.dependencies]
libc.workspace = true
tempfile.workspace = true
