## Bug Description
<!-- What happened? Describe the user-visible failure without including secrets. -->

## Command
<!--
Use the actual n3v3 CLI command that triggers the problem. Valid examples:
`n3v3 --version`, `n3v3 check path/to/repro.n3v3`, and
`n3v3 run path/to/repro.n3v3`.
-->
```console
$ n3v3 --version
$ n3v3 check path/to/repro.n3v3
# Or, for runtime behavior:
$ n3v3 run path/to/repro.n3v3
```

## Environment
- n3v3 version: output of `n3v3 --version`
- Platform: operating system and architecture

## Minimal Reproduction
```n3v3
<!-- Smallest complete source file that reproduces the issue. -->
```

## Expected Behavior
<!-- What should `n3v3 check` or `n3v3 run` do? -->

## Actual Behavior
<!-- What did the command do instead? Include the complete diagnostic if possible. -->

## Additional Context
<!-- Logs, relevant configuration, or a link to a minimal repository. Remove secrets and personal data. -->
